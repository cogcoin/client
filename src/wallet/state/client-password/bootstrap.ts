import type { ClientPasswordAgentBootstrapState } from "./types.js";

export function createAgentBootstrapState(
  state: ClientPasswordAgentBootstrapState,
): ClientPasswordAgentBootstrapState {
  return state;
}
